[pytest]
asyncio_mode = auto
# Set explicitly because pytest-asyncio warns on every run otherwise, and
# will change the default in a future release. `function` matches what the
# suite already relies on: each test gets a fresh event loop, and the
# in-memory SQLite engine in conftest is per-test.
asyncio_default_fixture_loop_scope = function
