global:
  scrape_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  - job_name: llmhell-api
    static_configs:
      - targets: ["api:8000"]

  # Add the real RunPod vLLM pods here once deployed, e.g.:
  # - job_name: vllm-planner
  #   static_configs:
  #     - targets: ["<runpod-host>:<port>"]
  #   metrics_path: /metrics
