# Docker reads this from the build context, which is ./frontend - a
# repo-root .dockerignore would be ignored entirely.
node_modules/
.next/
.turbo/
.git/
*.log

# Never bake local env files into an image layer.
.env
.env.*
!.env.example
